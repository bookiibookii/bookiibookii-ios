import SwiftUI

struct LibraryView: View {
    var body: some View {
        VStack {
            Text("서재")
                .font(.largeTitle)
                .foregroundStyle(.secondary)
        }
    }
}

#Preview {
    LibraryView()
}
